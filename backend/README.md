# Backend (FastAPI)

This folder contains the FastAPI backend for serving the CNN model.

## Structure:
- `main.py` - FastAPI app with prediction endpoint
- `model_loader.py` - Load and manage the trained model
- `utils.py` - Image preprocessing utilities
- `requirements.txt` - Python dependencies
